version: '3.4'

services:
  flask:
    build: .
    ports:
      - "5001:5001"
    environment:
      - FLASK_PORT=5001
    network_mode: "host"

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - flask


